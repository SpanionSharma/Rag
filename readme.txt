Create a virutal environment wherein the required dependencies are to be installed 
create another folder named documents where you put in the document(s) you want to interact with through the rag application
generate specific api keys from pinecone and the llm of choice in this case ChatOpenAI model is used thus the openAi key ,if not possible simply switch the imports to gemini based commands and get a free tier key from gemini's section
run the files post that
and began interaction with the local knowledge based chatbot
